<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head> 
<body>
    <% if(locals.user) { %>
        <h1>Welcome Back <%= user.username %> !</h1>
        <a href="/log-out">Log Out</a>
        <% if(locals.user.member != 'true') { %>
            <a href="/member">Click here to become a member!</a>
        <% } else { %>
            <a href="/new-message">Create a new message</a>
        <%}%>

   <% } else { %>
        <h1>Welcome! Please sign up or log in:</h1>
        <a href="/sign-up">Sign Up</a>
        <a href="/log-in">Log In</a>
    <% } %>

    <% messages.forEach((message) => { %>
        <ul>
            <%if (locals.user) { %>
                <%if (user.member == 'true') { %>
                    <li>From <%= message.username%> on <%= message.timestamp %>:</li>
                <%} else { %>
                    <li>From Anonymous:</li>
                <%}%>
            <% } else { %>
                <li>From Anonymous:</li>
            <% } %>
            <li> <%= message.text %></li>
        </ul>
    <% }); %>
</body>
</html>